# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Python (likely; the next developer may add language-specific entries)
__pycache__/
*.py[cod]
.venv/
venv/
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
.pnpm-store/
.npm/
dist/
build/

# Audio caches and generated artifacts (when the system exists)
/data/
/cache/
/runs/
*.wav
*.mp3
*.ogg
*.flac
# (research/ has no audio yet, but this rule will protect future work without
# touching the corpus, which is markdown + PDFs only)

# Local secrets / env
.env
.env.*
!.env.example
secrets/

# Decisions in flight
decisions/draft-*.md
